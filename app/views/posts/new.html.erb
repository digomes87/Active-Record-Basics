<%= form_with model: @post do |p|%>

  <%= p.label :title, "Title" %>
  <%= p.text_field :title %><br>

  <%= p.label :body, "Body" %>
  <%= p.text_field :body %><br>
  
  <%= p.submit %>

 <% end %> 